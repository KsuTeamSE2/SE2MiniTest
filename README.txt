コミットを使えるかテスト
参考にgithubを使いやすくするのに
SouceTreeからコミットしてみる。

- git —

git clone URL //リポジトリを取ってくる

git pull  //変更があるかの確認

//なにか変更したときのおまじない

git add [変更したファイル]

git commit -m “●●を変更しました”

git push origin master